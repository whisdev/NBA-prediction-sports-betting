{
  "name": "nba-machine-learning-sports-betting",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build:lib && npm run build:flask",
    "build:lib": "tsc -p tsconfig.lib.json",
    "build:flask": "tsc -p tsconfig.flask.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "python:main": "python3 main.py"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.6"
  }
}
